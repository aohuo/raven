# 已知问题

以下来自源码和 `database/order.sql` 的静态核对，未进行网站启动、SQL 导入或业务验收。

1. 密码仍按原样保存和比较，数据库字段是整数；缺少密码哈希与角色权限设计。
2. `search_customer_1.html` 的角色卡片与数据写在页面中，仅在浏览器中搜索、筛选，未从数据库读取。
3. 部分图片使用包含 `public` 的相对路径，与 `public/` 作为 Web 根目录的部署方式可能不一致；部分样式、脚本依赖 CDN。
4. 数据字典规划的招式帧数、受击框、录像上传、胜负统计、管理权限和版本历史，尚未发现完整实现及对应数据表。
5. 旧订单示例的商家页面和数据表仍保留用于兼容，不属于当前 GGST 角色与玩家资料管理流程。

## 已修正的增删改查问题

- 玩家新增、更新、删除统一使用 `customer` 表，并补齐菜单对应的控制器和页面。
- 角色新增、更新、删除统一使用 `character_name` 表；“最近更新角色”保留为独立查询页面。
- 模型字段、页面字段和 SQL 字段已经对齐，并为业务标识补充主键。
- 修正了角色查询字段、延迟更新模型导入、修改密码会话字段及旧商家模型缺失等问题。

本次 Git 整理只调整配置读取与文件组织，以上问题保留为后续修复清单。
